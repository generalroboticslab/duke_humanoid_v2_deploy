-r ../requirements.txt   # one pinned set for the whole tree; perception/ adds nothing of its own
# Its block there, for reference (the rest -- numpy, scipy, opencv-python, viser, trimesh, tyro, pynng, msgpack, PyYAML, matplotlib, tqdm -- is the control-stack block):
#   simplejpeg==1.9.0  pupil_apriltags==1.0.4.post11  moms-apriltag==2023.1.15  numba==0.65.1  pyrealsense2==2.58.1.10581
